export default async function handler(req,res){if(req.method!=='POST')return res.status(405).json({ok:false});try{const body=typeof req.body==='string'?JSON.parse(req.body):req.body||{};console.error('[Au-delà des Dys client error]',{message:String(body.message||'unknown').slice(0,500),stack:String(body.stack||'').slice(0,4000),url:String(body.url||'').slice(0,500),type:String(body.type||'client')});return res.status(200).json({ok:true})}catch{return res.status(400).json({ok:false})}}
