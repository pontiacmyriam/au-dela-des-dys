async function sendTransactionalEmail({to,subject,html}){const key=process.env.RESEND_API_KEY;if(!key){console.warn('RESEND_API_KEY missing; email skipped',{to,subject});return{ok:false,skipped:true}}const response=await fetch('https://api.resend.com/emails',{method:'POST',headers:{Authorization:`Bearer ${key}`,'Content-Type':'application/json'},body:JSON.stringify({from:process.env.EMAIL_FROM||'Au-delà des Dys <notifications@audeladesdys.fr>',to:[to],subject,html})});if(!response.ok)throw new Error(`Email service error ${response.status}`);return{ok:true}}module.exports={sendTransactionalEmail};
